__pycache__/
*.py[cod]
*$py.class
*.so
.Python
env/
venv/
ENV/
.venv
*.egg-info/
.eggs/
dist/
build/
.env
.DS_Store
*.log

.pytest_cache/

# Documentation/Notes (not needed in production)
*.txt
agennt builder sdk agent kodları.txt
agent builder sdk python versiyon.txt

# Build inputs must survive the *.txt rule above. requirements.txt only stayed in the
# repo because it was already tracked - Git does not re-ignore a tracked file - so a
# fresh clone-and-add would have dropped it and broken the Railway build.
!requirements.txt
